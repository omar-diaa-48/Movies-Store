
@model ShoppingCartViewModel

@if (Model.Order.OrderedMovies.Count > 0)
{
    <li>
        <a>
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart_status">
                @Model.Order.OrderedMovies.Count
            </span>
        </a>
    </li>
}